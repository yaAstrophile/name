<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moving to Our New Home</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Georgia', serif;
            overflow-x: hidden;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        /* Particle Background */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.6);
            border-radius: 50%;
            animation: float-particle 15s infinite;
        }

        @keyframes float-particle {
            0%, 100% {
                transform: translateY(0) translateX(0) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) translateX(100px) rotate(360deg);
                opacity: 0;
            }
        }

        .scene {
            display: none;
            min-height: 100vh;
            padding: 40px 20px;
            position: relative;
            z-index: 1;
        }

        .scene.active {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            animation: sceneEnter 1s ease-out;
        }

        @keyframes sceneEnter {
            from { 
                opacity: 0;
                transform: scale(0.9);
            }
            to { 
                opacity: 1;
                transform: scale(1);
            }
        }

        /* Scene 1: Old Home */
        .old-home {
            background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
        }

        .house {
            width: 400px;
            height: 350px;
            background: #fff5e6;
            border-radius: 20px;
            position: relative;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            cursor: pointer;
            transition: all 0.5s ease;
            animation: houseAppear 1s ease-out;
        }

        @keyframes houseAppear {
            from {
                transform: translateY(-100px) scale(0.5);
                opacity: 0;
            }
            to {
                transform: translateY(0) scale(1);
                opacity: 1;
            }
        }

        .house:hover {
            transform: scale(1.08) rotate(2deg);
            box-shadow: 0 30px 80px rgba(0,0,0,0.4);
        }

        .roof {
            width: 0;
            height: 0;
            border-left: 220px solid transparent;
            border-right: 220px solid transparent;
            border-bottom: 120px solid #ff6b9d;
            position: absolute;
            top: -100px;
            left: -20px;
            animation: roofSlide 1s ease-out 0.3s backwards;
        }

        @keyframes roofSlide {
            from {
                transform: translateY(-100px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .chimney {
            width: 30px;
            height: 60px;
            background: #c44569;
            position: absolute;
            top: -130px;
            right: 80px;
            border-radius: 5px 5px 0 0;
            animation: chimneyGrow 0.5s ease-out 0.5s backwards;
        }

        @keyframes chimneyGrow {
            from {
                height: 0;
            }
            to {
                height: 60px;
            }
        }

        .smoke {
            position: absolute;
            top: -20px;
            left: 50%;
            transform: translateX(-50%);
        }

        .smoke-particle {
            width: 15px;
            height: 15px;
            background: rgba(200, 200, 200, 0.6);
            border-radius: 50%;
            position: absolute;
            animation: smokeRise 3s infinite;
        }

        .smoke-particle:nth-child(1) { animation-delay: 0s; }
        .smoke-particle:nth-child(2) { animation-delay: 1s; }
        .smoke-particle:nth-child(3) { animation-delay: 2s; }

        @keyframes smokeRise {
            0% {
                transform: translateY(0) scale(0.5);
                opacity: 0;
            }
            50% {
                opacity: 0.8;
            }
            100% {
                transform: translateY(-80px) translateX(20px) scale(1.5);
                opacity: 0;
            }
        }

        .door {
            width: 80px;
            height: 120px;
            background: #c44569;
            border-radius: 10px 10px 0 0;
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            animation: doorAppear 0.5s ease-out 0.7s backwards;
        }

        @keyframes doorAppear {
            from {
                transform: translateX(-50%) scaleY(0);
            }
            to {
                transform: translateX(-50%) scaleY(1);
            }
        }

        .doorknob {
            width: 10px;
            height: 10px;
            background: gold;
            border-radius: 50%;
            position: absolute;
            right: 15px;
            top: 60px;
            animation: knobGleam 2s infinite;
        }

        @keyframes knobGleam {
            0%, 100% { box-shadow: 0 0 5px gold; }
            50% { box-shadow: 0 0 20px gold; }
        }

        .window {
            width: 70px;
            height: 70px;
            background: #a8e6cf;
            border: 4px solid #fff;
            border-radius: 5px;
            position: absolute;
            animation: windowPop 0.5s ease-out backwards;
        }

        .window:nth-child(2) { 
            top: 80px; 
            left: 50px;
            animation-delay: 0.9s;
        }
        .window:nth-child(3) { 
            top: 80px; 
            right: 50px;
            animation-delay: 1.1s;
        }

        @keyframes windowPop {
            from {
                transform: scale(0);
                opacity: 0;
            }
            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        .window-cross {
            position: absolute;
            background: #fff;
        }

        .window-cross:nth-child(1) {
            width: 2px;
            height: 100%;
            left: 50%;
            transform: translateX(-50%);
        }

        .window-cross:nth-child(2) {
            height: 2px;
            width: 100%;
            top: 50%;
            transform: translateY(-50%);
        }

        .current-name {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 24px;
            font-weight: bold;
            color: #c44569;
            text-align: center;
            z-index: 10;
            animation: nameGlow 2s ease-in-out infinite;
        }

        @keyframes nameGlow {
            0%, 100% {
                text-shadow: 0 0 10px rgba(196, 69, 105, 0.5);
                transform: translate(-50%, -50%) scale(1);
            }
            50% {
                text-shadow: 0 0 30px rgba(196, 69, 105, 0.8);
                transform: translate(-50%, -50%) scale(1.05);
            }
        }

        .history-photos {
            position: absolute;
            bottom: 20px;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            max-width: 350px;
            justify-content: center;
        }

        .photo {
            background: white;
            padding: 8px;
            border-radius: 5px;
            font-size: 11px;
            color: #666;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            transform: rotate(-2deg);
            animation: photoFall 0.5s ease-out backwards;
            transition: all 0.3s ease;
        }

        .photo:hover {
            transform: scale(1.2) rotate(0deg);
            z-index: 10;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .photo:nth-child(even) { transform: rotate(2deg); }
        .photo:nth-child(1) { animation-delay: 1.3s; }
        .photo:nth-child(2) { animation-delay: 1.4s; }
        .photo:nth-child(3) { animation-delay: 1.5s; }
        .photo:nth-child(4) { animation-delay: 1.6s; }
        .photo:nth-child(5) { animation-delay: 1.7s; }
        .photo:nth-child(6) { animation-delay: 1.8s; }
        .photo:nth-child(7) { animation-delay: 1.9s; }

        @keyframes photoFall {
            from {
                transform: translateY(-200px) rotate(180deg);
                opacity: 0;
            }
            to {
                transform: translateY(0) rotate(-2deg);
                opacity: 1;
            }
        }

        .message {
            max-width: 600px;
            text-align: center;
            margin-top: 40px;
            padding: 25px;
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            font-size: 18px;
            color: #333;
            line-height: 1.6;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            animation: messageSlide 0.8s ease-out 2s backwards;
        }

        @keyframes messageSlide {
            from {
                transform: translateY(50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .instruction {
            margin-top: 20px;
            font-size: 14px;
            color: #666;
            font-style: italic;
            animation: blink 2s infinite;
        }

        @keyframes blink {
            0%, 50%, 100% { opacity: 1; }
            25%, 75% { opacity: 0.5; }
        }

        /* Scene 2: Moving */
        .moving-scene {
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
        }

        .road {
            width: 100%;
            height: 20px;
            background: repeating-linear-gradient(
                90deg,
                #333 0px,
                #333 40px,
                transparent 40px,
                transparent 80px
            );
            position: absolute;
            bottom: 200px;
            animation: roadMove 2s linear infinite;
        }

        @keyframes roadMove {
            from { background-position: 0 0; }
            to { background-position: 80px 0; }
        }

        .moving-truck {
            width: 300px;
            height: 150px;
            background: #ff6b9d;
            border-radius: 10px;
            position: relative;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 10px 40px rgba(0,0,0,0.3);
            animation: truckBounce 1s ease-in-out infinite;
        }

        @keyframes truckBounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .moving-truck:hover {
            transform: translateY(-15px) scale(1.05);
            box-shadow: 0 20px 50px rgba(0,0,0,0.4);
        }

        .truck-cabin {
            width: 100px;
            height: 150px;
            background: #c44569;
            border-radius: 10px 0 0 10px;
            position: absolute;
            left: 0;
        }

        .truck-window {
            width: 50px;
            height: 40px;
            background: linear-gradient(135deg, #a8e6cf 0%, #ffffff 100%);
            border-radius: 5px;
            position: absolute;
            top: 30px;
            left: 25px;
            animation: windowReflect 3s ease-in-out infinite;
        }

        @keyframes windowReflect {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .wheel {
            width: 50px;
            height: 50px;
            background: #333;
            border-radius: 50%;
            position: absolute;
            bottom: -25px;
            border: 5px solid #666;
            animation: wheelSpin 1s linear infinite;
        }

        @keyframes wheelSpin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .wheel:nth-child(1) { left: 30px; }
        .wheel:nth-child(2) { right: 30px; }

        .wheel::before {
            content: '';
            position: absolute;
            width: 4px;
            height: 100%;
            background: #999;
            left: 50%;
            transform: translateX(-50%);
        }

        .wheel::after {
            content: '';
            position: absolute;
            height: 4px;
            width: 100%;
            background: #999;
            top: 50%;
            transform: translateY(-50%);
        }

        .boxes {
            position: absolute;
            top: 30px;
            right: 40px;
            display: flex;
            gap: 10px;
        }

        .box {
            width: 40px;
            height: 40px;
            background: #ffd89b;
            border: 2px solid #f093fb;
            position: relative;
            animation: boxShake 0.5s ease-in-out infinite alternate;
        }

        .box:nth-child(1) { animation-delay: 0s; }
        .box:nth-child(2) { animation-delay: 0.1s; }
        .box:nth-child(3) { animation-delay: 0.2s; }

        @keyframes boxShake {
            from { transform: translateY(0); }
            to { transform: translateY(-5px); }
        }

        .box::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 2px;
            background: #f093fb;
        }

        .box::after {
            content: '';
            position: absolute;
            left: 50%;
            top: 0;
            bottom: 0;
            width: 2px;
            background: #f093fb;
        }

        .clouds {
            position: absolute;
            top: 50px;
            width: 100%;
            height: 100px;
        }

        .cloud {
            position: absolute;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 100px;
            animation: cloudFloat 20s linear infinite;
        }

        .cloud:nth-child(1) {
            width: 100px;
            height: 40px;
            top: 20px;
            left: -100px;
            animation-delay: 0s;
        }

        .cloud:nth-child(2) {
            width: 80px;
            height: 35px;
            top: 60px;
            left: -80px;
            animation-delay: 5s;
        }

        @keyframes cloudFloat {
            from { transform: translateX(0); }
            to { transform: translateX(calc(100vw + 200px)); }
        }

        /* Scene 3: New Home */
        .new-home {
            background: linear-gradient(135deg, #ffeaa7 0%, #fdcb6e 100%);
        }

        .new-house {
            width: 450px;
            height: 400px;
            background: #fff;
            border-radius: 20px;
            position: relative;
            box-shadow: 0 25px 70px rgba(0,0,0,0.3);
            cursor: pointer;
            transition: all 0.5s ease;
            animation: newHouseGrow 1.5s ease-out;
        }

        @keyframes newHouseGrow {
            from {
                transform: scale(0);
                opacity: 0;
                rotate: -180deg;
            }
            to {
                transform: scale(1);
                opacity: 1;
                rotate: 0deg;
            }
        }

        .new-house:hover {
            transform: scale(1.05);
            box-shadow: 0 35px 90px rgba(0,0,0,0.4);
        }

        .new-roof {
            width: 0;
            height: 0;
            border-left: 245px solid transparent;
            border-right: 245px solid transparent;
            border-bottom: 140px solid #6c5ce7;
            position: absolute;
            top: -120px;
            left: -20px;
        }

        .stars {
            position: absolute;
            top: -150px;
            left: 0;
            right: 0;
        }

        .star {
            position: absolute;
            color: gold;
            font-size: 20px;
            animation: starTwinkle 2s ease-in-out infinite;
        }

        .star:nth-child(1) { left: 50px; animation-delay: 0s; }
        .star:nth-child(2) { left: 150px; top: 20px; animation-delay: 0.5s; }
        .star:nth-child(3) { right: 150px; animation-delay: 1s; }
        .star:nth-child(4) { right: 50px; top: 20px; animation-delay: 1.5s; }

        @keyframes starTwinkle {
            0%, 100% {
                opacity: 1;
                transform: scale(1);
            }
            50% {
                opacity: 0.3;
                transform: scale(0.8);
            }
        }

        .new-door {
            width: 100px;
            height: 140px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 15px 15px 0 0;
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            cursor: pointer;
            transition: all 0.5s ease;
            animation: doorPulse 2s ease-in-out infinite;
        }

        @keyframes doorPulse {
            0%, 100% {
                box-shadow: 0 0 20px rgba(102, 126, 234, 0.5);
            }
            50% {
                box-shadow: 0 0 40px rgba(102, 126, 234, 1);
            }
        }

        .new-door:hover {
            transform: translateX(-50%) scale(1.1);
            box-shadow: 0 0 50px rgba(102, 126, 234, 1);
        }

        .heart-decoration {
            position: absolute;
            top: -80px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 50px;
            animation: heartFloat 3s ease-in-out infinite;
        }

        @keyframes heartFloat {
            0%, 100% {
                transform: translateX(-50%) translateY(0) scale(1);
            }
            50% {
                transform: translateX(-50%) translateY(-20px) scale(1.2);
            }
        }

        .garden {
            position: absolute;
            bottom: -20px;
            left: 0;
            right: 0;
            height: 40px;
            display: flex;
            justify-content: space-around;
            align-items: flex-end;
        }

        .flower {
            font-size: 30px;
            animation: flowerGrow 2s ease-out backwards;
        }

        .flower:nth-child(1) { animation-delay: 1.5s; }
        .flower:nth-child(2) { animation-delay: 1.7s; }
        .flower:nth-child(3) { animation-delay: 1.9s; }
        .flower:nth-child(4) { animation-delay: 2.1s; }

        @keyframes flowerGrow {
            from {
                transform: scale(0) rotate(-90deg);
                opacity: 0;
            }
            to {
                transform: scale(1) rotate(0deg);
                opacity: 1;
            }
        }

        /* Scene 4: Name Reveal */
        .reveal-scene {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            position: relative;
            overflow: hidden;
        }

        .sparkles {
            position: absolute;
            width: 100%;
            height: 100%;
        }

        .sparkle {
            position: absolute;
            color: gold;
            font-size: 30px;
            animation: sparkleFloat 3s ease-in-out infinite;
        }

        @keyframes sparkleFloat {
            0% {
                transform: translateY(0) rotate(0deg);
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100px) rotate(360deg);
                opacity: 0;
            }
        }

        .reveal-container {
            text-align: center;
            padding: 60px;
            background: rgba(255,255,255,0.95);
            border-radius: 30px;
            box-shadow: 0 30px 80px rgba(0,0,0,0.3);
            max-width: 700px;
            animation: revealPop 1s ease-out;
            position: relative;
            z-index: 10;
        }

        @keyframes revealPop {
            0% {
                transform: scale(0) rotate(-180deg);
                opacity: 0;
            }
            60% {
                transform: scale(1.1) rotate(10deg);
            }
            100% {
                transform: scale(1) rotate(0deg);
                opacity: 1;
            }
        }

        .reveal-title {
            font-size: 52px;
            color: #c44569;
            margin-bottom: 30px;
            animation: titleGlow 2s ease-in-out infinite, titleBounce 3s ease-in-out infinite;
            background: linear-gradient(45deg, #c44569, #f093fb, #c44569);
            background-size: 200% 200%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        @keyframes titleGlow {
            0%, 100% { 
                filter: drop-shadow(0 0 20px rgba(196, 69, 105, 0.5));
            }
            50% { 
                filter: drop-shadow(0 0 40px rgba(196, 69, 105, 1));
            }
        }

        @keyframes titleBounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .meaning {
            font-size: 24px;
            color: #666;
            margin-bottom: 20px;
            font-style: italic;
            animation: fadeInUp 1s ease-out 0.5s backwards;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .explanation {
            font-size: 20px;
            color: #333;
            line-height: 1.8;
            margin-top: 30px;
            animation: fadeInUp 1s ease-out 1s backwards;
        }

        .continue-btn {
            margin-top: 30px;
            padding: 15px 40px;
            font-size: 18px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
            animation: fadeInUp 1s ease-out 1.5s backwards, btnPulse 2s ease-in-out 2.5s infinite;
        }

        @keyframes btnPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .continue-btn:hover {
            transform: translateY(-5px) scale(1.1);
            box-shadow: 0 20px 50px rgba(102, 126, 234, 0.6);
        }

        /* Scene 5: Final Message */
        .final-scene {
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
        }

        .final-message-container {
            max-width: 700px;
            padding: 50px;
            background: white;
            border-radius: 30px;
            box-shadow: 0 30px 80px rgba(0,0,0,0.3);
            text-align: center;
            animation: finalAppear 1.5s ease-out;
        }

        @keyframes finalAppear {
            0% {
                transform: scale(0.5) rotateY(-180deg);
                opacity: 0;
            }
            100% {
                transform: scale(1) rotateY(0deg);
                opacity: 1;
            }
        }

        .final-message {
            font-size: 24px;
            color: #333;
            line-height: 2;
            font-family: 'Georgia', serif;
        }

        .final-message br {
            display: block;
            content: "";
            margin: 10px 0;
        }

        .hearts-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1000;
        }

        .heart {
            position: absolute;
            font-size: 30px;
            animation: heartRise 4s ease-in infinite;
            opacity: 0;
        }

        @keyframes heartRise {
            0% {
                bottom: -50px;
                opacity: 1;
                transform: translateX(0) rotate(0deg);
            }
            100% {
                bottom: 110%;
                opacity: 0;
                transform: translateX(100px) rotate(360deg);
            }
        }

        /* Music Control */
        .music-control {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            z-index: 10000;
            transition: all 0.3s ease;
            animation: musicFloat 3s ease-in-out infinite;
        }

        @keyframes musicFloat {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .music-control:hover {
            transform: scale(1.1);
            box-shadow: 0 15px 40px rgba(0,0,0,0.4);
        }

        .music-icon {
            font-size: 30px;
            color: white;
        }

        .music-waves {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
        }

        .music-wave {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border: 2px solid rgba(255, 255, 255, 0.6);
            border-radius: 50%;
            animation: waveExpand 2s ease-out infinite;
        }

        .music-wave:nth-child(2) { animation-delay: 0.5s; }
        .music-wave:nth-child(3) { animation-delay: 1s; }

        @keyframes waveExpand {
            0% {
                transform: scale(1);
                opacity: 1;
            }
            100% {
                transform: scale(2);
                opacity: 0;
            }
        }

        @media (max-width: 768px) {
            .house, .new-house {
                width: 300px;
                height: 280px;
            }
            
            .roof {
                border-left: 170px solid transparent;
                border-right: 170px solid transparent;
            }
            
            .new-roof {
                border-left: 170px solid transparent;
                border-right: 170px solid transparent;
            }
            
            .reveal-title {
                font-size: 32px;
            }
            
            .final-message {
                font-size: 18px;
            }
            
            .moving-truck {
                width: 250px;
            }
        }
    </style>
</head>
<body>
    <!-- Particles Background -->
    <div class="particles" id="particlesContainer"></div>

    <!-- Scene 1: Old Home -->
    <div class="scene active old-home" id="scene1">
        <div class="house" onclick="playSound('door'); nextScene(2)">
            <div class="chimney">
                <div class="smoke">
                    <div class="smoke-particle"></div>
                    <div class="smoke-particle"></div>
                    <div class="smoke-particle"></div>
                </div>
            </div>
            <div class="roof"></div>
            <div class="window">
                <div class="window-cross"></div>
                <div class="window-cross"></div>
            </div>
            <div class="window">
                <div class="window-cross"></div>
                <div class="window-cross"></div>
            </div>
            <div class="current-name">Nebula Queen Risa 👑</div>
            <div class="door">
                <div class="doorknob"></div>
            </div>
            <div class="history-photos">
                <div class="photo">angrybird</div>
                <div class="photo">Dipaboli</div>
                <div class="photo">Mitsuha</div>
                <div class="photo">Hinata</div>
                <div class="photo">Alladi Meye</div>
                <div class="photo">Turu</div>
                <div class="photo">class 4</div>
            </div>
        </div>
        <div class="message">
            Every name I've called you has been a chapter in our beautiful story. From the very first time I met you, through all the moments we've shared, each name held a special memory.
            <div class="instruction">✨ Click on the house to continue ✨</div>
        </div>
    </div>

    <!-- Scene 2: Moving -->
    <div class="scene moving-scene" id="scene2">
        <div class="clouds">
            <div class="cloud"></div>
            <div class="cloud"></div>
        </div>
        <div class="road"></div>
        <div class="moving-truck" onclick="playSound('truck'); nextScene(3)">
            <div class="truck-cabin">
                <div class="truck-window"></div>
            </div>
            <div class="boxes">
                <div class="box"></div>
                <div class="box"></div>
                <div class="box"></div>
            </div>
            <div class="wheel"></div>
            <div class="wheel"></div>
        </div>
        <div class="message">
            Now we're moving to a new place together. The memories we've created in our old home will always stay with us, but our future holds even more beautiful moments. Let me take you to our new home...
            <div class="instruction">🚚 Click on the truck to continue 🚚</div>
        </div>
    </div>

    <!-- Scene 3: New Home -->
    <div class="scene new-home" id="scene3">
        <div class="new-house">
            <div class="stars">
                <div class="star">⭐</div>
                <div class="star">✨</div>
                <div class="star">⭐</div>
                <div class="star">✨</div>
            </div>
            <div class="heart-decoration">💕</div>
            <div class="new-roof"></div>
            <div class="window">
                <div class="window-cross"></div>
                <div class="window-cross"></div>
            </div>
            <div class="window">
                <div class="window-cross"></div>
                <div class="window-cross"></div>
            </div>
            <div class="new-door" onclick="playSound('door'); nextScene(4)"></div>
            <div class="garden">
                <div class="flower">🌸</div>
                <div class="flower">🌺</div>
                <div class="flower">🌷</div>
                <div class="flower">🌹</div>
            </div>
        </div>
        <div class="message">
            Welcome to our new home! This is where your new name awaits—a name that truly captures what you mean to me and the journey we're embarking on together...
            <div class="instruction">🚪 Click on the door to reveal 🚪</div>
        </div>
    </div>

    <!-- Scene 4: Name Reveal -->
    <div class="scene reveal-scene" id="scene4">
        <div class="sparkles" id="sparklesContainer"></div>
        <div class="reveal-container">
            <div class="reveal-title">L'amour de ma vie</div>
            <div class="meaning">"The Love of My Life"</div>
            <div class="explanation">
                This is your new name, because that's exactly what you are to me—<strong>the love of my life</strong>. You are my happiness, my peace, and my forever. Every moment with you is a treasure, and I want you to know how deeply and completely you are cherished. ✨💕
            </div>
            <button class="continue-btn" onclick="playSound('sparkle'); nextScene(5); startHearts()">Continue to My Message ❤️</button>
        </div>
    </div>

    <!-- Scene 5: Final Message -->
    <div class="scene final-scene" id="scene5">
        <div class="final-message-container">
            <div class="final-message">
                I'm not going to leave you ever, <em>insha'Allah</em>.<br><br>
                The place might change, time might change,<br>
                but I will not change.<br><br>
                You will find me right by your side,<br>
                through every season, every moment,<br>
                forever and always.<br><br>
                ❤️ <strong>Forever yours</strong> ❤️
            </div>
        </div>
    </div>

    <div class="hearts-animation" id="heartsContainer"></div>

    <!-- Music Control -->
    <div class="music-control" onclick="toggleMusic()" id="musicControl">
        <div class="music-waves" id="musicWaves">
            <div class="music-wave"></div>
            <div class="music-wave"></div>
            <div class="music-wave"></div>
        </div>
        <div class="music-icon" id="musicIcon">🎵</div>
    </div>

    <script>
        let currentScene = 1;
        let audioContext;
        let musicPlaying = false;
        let oscillators = [];

        // Initialize
        window.onload = function() {
            createParticles();
            startBackgroundMusic();
        };

        function createParticles() {
            const container = document.getElementById('particlesContainer');
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.width = Math.random() * 5 + 2 + 'px';
                particle.style.height = particle.style.width;
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 15 + 's';
                particle.style.animationDuration = (Math.random() * 10 + 10) + 's';
                container.appendChild(particle);
            }
        }

        function nextScene(sceneNum) {
            document.getElementById('scene' + currentScene).classList.remove('active');
            currentScene = sceneNum;
            document.getElementById('scene' + currentScene).classList.add('active');
            
            if (sceneNum === 4) {
                createSparkles();
            }
        }

        function createSparkles() {
            const container = document.getElementById('sparklesContainer');
            setInterval(() => {
                const sparkle = document.createElement('div');
                sparkle.className = 'sparkle';
                sparkle.textContent = Math.random() > 0.5 ? '✨' : '⭐';
                sparkle.style.left = Math.random() * 100 + '%';
                sparkle.style.top = Math.random() * 100 + '%';
                sparkle.style.animationDuration = (Math.random() * 2 + 2) + 's';
                container.appendChild(sparkle);

                setTimeout(() => {
                    sparkle.remove();
                }, 3000);
            }, 300);
        }

        function playSound(type) {
            if (!audioContext) {
                audioContext = new (window.AudioContext || window.webkitAudioContext)();
            }
            
            const oscillator = audioContext.createOscillator();
            const gainNode = audioContext.createGain();

            oscillator.connect(gainNode);
            gainNode.connect(audioContext.destination);

            if (type === 'door') {
                oscillator.frequency.value = 300;
                oscillator.type = 'sine';
                gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
                gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.5);
                oscillator.start(audioContext.currentTime);
                oscillator.stop(audioContext.currentTime + 0.5);
            } else if (type === 'truck') {
                oscillator.frequency.value = 120;
                oscillator.type = 'sawtooth';
                gainNode.gain.setValueAtTime(0.2, audioContext.currentTime);
                gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 1);
                oscillator.start(audioContext.currentTime);
                oscillator.stop(audioContext.currentTime + 1);
            } else if (type === 'sparkle') {
                oscillator.frequency.value = 1000;
                oscillator.type = 'sine';
                gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
                gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.3);
                oscillator.start(audioContext.currentTime);
                oscillator.stop(audioContext.currentTime + 0.3);
            }
        }

        function startHearts() {
            const container = document.getElementById('heartsContainer');
            setInterval(() => {
                const heart = document.createElement('div');
                heart.className = 'heart';
                const hearts = ['❤️', '💕', '💖', '💗', '💝'];
                heart.textContent = hearts[Math.floor(Math.random() * hearts.length)];
                heart.style.left = Math.random() * 100 + '%';
                heart.style.animationDelay = Math.random() * 2 + 's';
                heart.style.fontSize = (Math.random() * 20 + 20) + 'px';
                container.appendChild(heart);

                setTimeout(() => {
                    heart.remove();
                }, 4000);
            }, 200);
        }

        function startBackgroundMusic() {
            if (!audioContext) {
                audioContext = new (window.AudioContext || window.webkitAudioContext)();
            }

            // Create a pleasant melody
            const notes = [523.25, 587.33, 659.25, 698.46, 783.99, 880.00]; // C5, D5, E5, F5, G5, A5
            let noteIndex = 0;

            function playNote() {
                if (!musicPlaying) return;

                const oscillator = audioContext.createOscillator();
                const gainNode = audioContext.createGain();

                oscillator.connect(gainNode);
                gainNode.connect(audioContext.destination);

                oscillator.frequency.value = notes[noteIndex % notes.length];
                oscillator.type = 'sine';
                
                gainNode.gain.setValueAtTime(0, audioContext.currentTime);
                gainNode.gain.linearRampToValueAtTime(0.1, audioContext.currentTime + 0.1);
                gainNode.gain.linearRampToValueAtTime(0, audioContext.currentTime + 0.9);

                oscillator.start(audioContext.currentTime);
                oscillator.stop(audioContext.currentTime + 1);

                noteIndex++;
                setTimeout(playNote, 600);
            }

            musicPlaying = true;
            document.getElementById('musicWaves').style.display = 'block';
            playNote();
        }

        function toggleMusic() {
            musicPlaying = !musicPlaying;
            const icon = document.getElementById('musicIcon');
            const waves = document.getElementById('musicWaves');
            
            if (musicPlaying) {
                icon.textContent = '🎵';
                waves.style.display = 'block';
                startBackgroundMusic();
            } else {
                icon.textContent = '🔇';
                waves.style.display = 'none';
            }
        }
    </script>
</body>
</html>
